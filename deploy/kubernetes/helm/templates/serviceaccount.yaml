{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "orb-chrysa.serviceAccountName" . }}
  namespace: {{ include "orb-chrysa.namespace" . }}
  labels:
    {{- include "orb-chrysa.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
